name: hello-world
on: push
workflow_dispatch:

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
          echo "Hello world"
          echo "test-multi line script"
